version: '3.8'

services:
  lgtv-test:
    build: .
    container_name: lgtv-network-test
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=test
    networks:
      - lgtv-test-network
    command: ["node", "dist/test_https.js"]

networks:
  lgtv-test-network:
    driver: bridge
